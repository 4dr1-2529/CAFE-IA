# URL del backend en producción (sin /api al final)
VITE_API_BASE_URL=http://localhost:3029

# Mostrar aviso de credenciales demo en login (solo desarrollo; false en producción)
VITE_SHOW_DEMO_CREDENTIALS=false
