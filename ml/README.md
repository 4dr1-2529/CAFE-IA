# Módulo ML — Café Sostenible AI

Evidencia académica de Machine Learning (PMV3).

## Uso

```bash
cd ml
pip install -r requirements.txt
python train_model.py
```

Genera `models/metrics.json` y `data/dataset_cafe.csv`.
