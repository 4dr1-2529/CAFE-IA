# SQLite v1 — eliminado

Los archivos legacy (`database.js`, `routes/*.js`, `database.sqlite`) fueron **eliminados** en v2.0.

Use únicamente:
- `server.js` → `src/app.js`
- MySQL (`backend/sql/schema.sql`)
